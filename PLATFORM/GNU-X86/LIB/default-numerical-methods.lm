
not := λ(: x U64). (: (
   (let r 0_u64)
   (if (==( x 0_u64 )) (
      (set r 1_u64)
   ) ())
   r
) U64);

not := λ(: x U8). (: (
   (let r 0_u64)
   (if (==( x 0_u8 )) (
      (set r 1_u64)
   ) ())
   r
) U64);

