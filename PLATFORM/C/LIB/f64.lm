
atom suffix F64 _f64;

print := λ(: x F64). (: (
   (printf( '%lf_s x )) ()
) Nil);

max := λ(: x F64)(: y F64). (: (
   (if (>=( x y )) x y)
) F64);

min := λ(: x F64)(: y F64). (: (
   (if (<=( x y )) x y)
) F64);

