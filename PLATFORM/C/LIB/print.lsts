
let print(e: x): Nil = (
   print( stdout(), e );
);

let eprint(e: x): Nil = (
   print( stderr(), e );
);

let fprint(io: IO::File, e: x): Nil = (
   print( io, e );
);

interface self implements DefaultPrintable {
   let print( f: IO::File, p: self ): Nil;
}
