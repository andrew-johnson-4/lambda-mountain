
onto-stack-size := λ(: tt Type). (: (
   (let r 0_u64)
   (match tt (
      ()
      ( (TAnd( t1 t2 )) (set r (max( (onto-stack-size t1) (onto-stack-size t2) ))) )
      ( (TGround( 'Cons_s (LCons( t1 (LCons( t2 LEOF )) )) )) (
         (if (is-onto-stack t1) (set r (sizeof-aligned t1)) ())
         (if (is-onto-stack t2) (set r (+( r (sizeof-aligned t2) ))) ())
      ))
      ( _ () )
   ))
   r
) U64);

