
with-tag := λ(: tt Type). (: (
   (let cls-tt (.with-only-class tt))
   (if (non-zero cls-tt) (
      (let tag (.only-child cls-tt))
      (if (head-string tag) (
         (set tt (&&( tt (t1( (+( 'Tag::_s tag )) )) )))
      ) ())
   ) ())
   tt
) Type);
