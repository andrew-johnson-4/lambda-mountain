
let denormalize(tt: Type): Type = (
   tt.rewrite-type-alias.enrich
);

let denormalize-strong(tt: Type): Type = (
   print("Denormalize Strong Begin\n");
   let return = tt.rewrite-type-alias.enrich-strong;
   print("Denormalize Strong End\n");
   return
);
