
fragment::new := λ . (: (
   (let r (Fragment(
      ASTEOF
      (close(: LEOF List<Tuple<String,S>>))
      0_i64
      TAny
      (close(fragment-context::new()))
      (: LEOF List<Fragment[]>)
   )))
   r
) Fragment);
