
let union(ctx: FContext, tctx: TContext): FContext = (
   let r = ctx;
   for Tuple{ k=first, vt=second } in phi-ctx.get-or([] :: List<Tuple<CString,Type,AST>>) {
      r = FCtxBind{ close(r), k, TAny, mk-fragment() };
   };
   r
);
