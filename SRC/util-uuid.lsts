
let uuid-counter = 0_u32;

# 0 is not a valid id
let iuid(): U32 = (
   uuid-counter = uuid-counter + 1;
   uuid-counter;
);

let uuid(): CString = (
   c"uuid__" + iuid().to-hex.into(type(CString));
);

