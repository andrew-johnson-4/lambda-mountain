
let .bind(ctx: Maybe<AContext>, key: CString, val: AST): Maybe<AContext> = (
   Some( cons( Tuple(key,val), ctx.get-or([] : AContext) ) )
);

let .bind(ctx: AContext, key: CString, val: AST): AContext = (
   cons( Tuple(key,val), ctx )
);
