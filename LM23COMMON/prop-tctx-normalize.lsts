
let .normalize(tctx: TypeContext?): TypeContext? = (
   let new-trs = [] : List<TypeContextRow>;
   for tr in tctx.get-or(mk-tctx()).tctx.reverse {
      new-trs = cons(TypeContextRow(tr.key, tr.direct-type.normalize.without-phi-keep-state, tr.direct-type.normalize.without-phi-keep-state, tr.blame), new-trs);
   };
   tctx.with-tctx(new-trs);
);
