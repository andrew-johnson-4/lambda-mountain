
let .apply-move-args(tctx: TypeContext?, params-type: Type, args-type: Type, blame: AST): TypeContext? = (
   let move-args = tctx.phi-move-args(params-type, args-type, blame);
   for PhiContextRow{pn=phi-id, pt=phi-tt, pblame=blame} in move-args.get-or(mk-tctx()).pctx {
      tctx = tctx.bind-phi(pn, pt, pblame);
   };
   tctx
);
