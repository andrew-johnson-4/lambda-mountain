
let .into(tctx: Maybe<TypeContext>, tt: Type<String>): String = (
   let result = "";
   for list tr in tctx.get-or(mk-tctx()).tctx {
      result = result + "\{tr.key} : \{tr.direct-type}\n";
   };
   result
);
