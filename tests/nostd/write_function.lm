
fib := λ(: x U8). (
   1u8
#   if (<= x 2u8) 1
#   (+(
#      (fib( (-( x 1u8 )) ))
#      (fib( (-( x 2u8 )) ))
#   ))
);

main := (
   (let cs 108u8)
   (set cs 100u8)
#   (set cs (fib 10u8))
   (mov( 1u64 RAX )) # write
   (mov( 1u64 RDI )) # stdout
   (mov( (& cs) RSI )) # data
   (mov( 2u64 RDX )) # length
   (syscall())
);
