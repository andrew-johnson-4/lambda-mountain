#fib := λ(: x U64). (
#   if (< x 2) 1
#);

#main := print (fib 35u64)

fib := λ(: x U64). ();
#   (-( x 1u64 ))
#   (fib( - x 1u64 ))
#   (+ (fib (- x 1u64)) (fib (- x 2u64)))

main := fib( 123u64 );
