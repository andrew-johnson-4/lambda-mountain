
fib := λ(: x U64). (: (
   x # put result into %r12
   (gensym-label low)
   (cmp( (: 1 (Fragment U64)) (: R12 (Fragment Reg64)) ))
   (jle( low ))
   (label low)
) U64);

main := fib( (: 35 U64) );
