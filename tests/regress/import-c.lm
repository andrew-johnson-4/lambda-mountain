
import std/default.lsts;
import math.h;
import stdlib.h;

main := λ. (: (
   (print(as (strtod(
      (as (clone-rope(SAtom '99_s)) C_char_*)
      (as 0_u64 C_char_*_*)
   )) U64))
   (print(as (strtod(
      (as (clone-rope(SAtom '123_s)) C_char_*)
      (as 0_u64 C_char_*_*)
   )) U64))
) Nil);
