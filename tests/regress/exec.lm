
import LIB/default.lm;

main := λ. (: (
   (let argv (const-cons(
       (as 'cpp_s U8[])
       (as '-include_s U8[])
       (as 'stdio.h_s U8[])
       (as '/dev/null_s U8[])
   )))
   # (execvp( (as 'cpp_s U8[]) argv ))
   ()
) Nil);
