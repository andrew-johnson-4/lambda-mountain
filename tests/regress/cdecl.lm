
import LIB/default.lm;

type XY X | (Y( U64 ));
type XYZ (Z( XY , U64 ));

x1 := (: (Z( X 1_u64 )) XYZ);

f1 := λ(: abc XYZ). (: (
   abc
) XYZ);

f2 := λ. (: (
   x1
) XYZ);

print := λ(: abc XYZ). (: (
   (match abc (
      ()
      ( (Z( X x )) (
         (print x)
      ))
      ( (Z( (Y( x )) y )) (tail(
         (print x)
         (print y)
      )))
   ))
) Nil);

main := λ. (: (tail(
   (print 'F1\s_s)
   (print(f1 x1))
   (print '\nF2\s_s)
   (print(f2()))
)) Nil);

