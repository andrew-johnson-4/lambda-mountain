
import lib2/core/baremetal.lsts;

type phi AState = First{promise:'a} | Second;
type A implied phi AState::First<'a> = {};

let f(a: A+AState::First<'a>): A+AState::Second = a as AState::Second;
let g(a: A+AState::Second): Nil = ();

g(f(A));
