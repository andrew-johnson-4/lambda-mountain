
import lib2/core/bedrock.lsts;

type T = { tag:CString };

let .ground-tag-and-arity(tt: T): (CString,U64) = (
   match tt {
      T { tag:c"Array" } => (c"Array", 2_u64);
      T { tag=tag } => (tag, 1_u64);
   }
);
