
import lib2/core/bedrock.lsts;

assert( (1,2) == (1,2) );
assert( (1,1) < (1,2) );
assert( (1,1) < (2,1) );
assert( safe-alloc-block-count == 0 );

assert( ("1","2") == ("1","2") );
#assert( ("1","1") < ("1","2") );
#assert( ("1","1") < ("2","1") );
assert( safe-alloc-block-count == 0 );

