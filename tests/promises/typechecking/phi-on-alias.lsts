
import lib2/core/bedrock.lsts;

type alias AContext = List<(CString,CString)>;

let substitute(ctx: AContext, tt: CString): CString = tt;

let apply(ctx: AContext): Type = (
   substitute(ctx,substitute(ctx,1));
);
