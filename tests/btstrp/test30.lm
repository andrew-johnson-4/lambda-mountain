
import LIB/default-instruction-set.lm.bak;
import LIB/default-primitives.lm;
import LIB/default-stdlib.lm;
import LIB/default-rules.lm;

type T (T( U64 , String ));

ts := λ(: s String). (: (tail(
   (let x 123_u64)
   (let t (T( 1_u64 s )))
   t
)) T);

main := λ. (: (
   (match (ts 'A_s) (
      ()
      ( (T( x y )) (tail(
         (print x)
         (print y)
      )))
   ))
) Nil);
