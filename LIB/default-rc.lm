
type Rc<a> (Rc( Tuple<U64,a>[] ));

rc::new := λ(: v x). (: (
   (let inner (Tuple( 1_u64 v )))
   (let rc (Rc( (close inner) )))
   rc
) Rc<x>);

#del := λ(: v Rc<x>). (: (
#   (let c (.2(open(.1 v))) )
#   (print 'Del\sCount\s_s)(print c)(print '\n_s)
#) Rc<x>);

#inc := λ(: v Rc<x>). (: (
#   (let c (.2(open(.1 v))) )
#   (print 'Inc\sCount\s_s)(print c)(print '\n_s)
#) Rc<x>);
