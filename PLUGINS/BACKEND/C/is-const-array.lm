
is-const-array := λ(: tt Type). (: (
   (let r 0_u64)
   (match tt (
      ()
      ( (TAnd( conjugate )) (
         (let result 0_u64)
         (for-each-v (c in conjugate) (
            (set result (||( result (is-const-array( c )) )))
         ))
         result
      ))
      ( (TGround( 'Array_s (LCons( array-size (LCons( array-base _ )) )) )) (set r (.is-t( array-size 'CONST_s ))) )
      ( _ () )
   ))
   r
) U64);
